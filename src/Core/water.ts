import { Item } from "./Item"

export class Water extends Item {
    constructor(
        category: string,
        price: number,
        description: string,
        id: number
    ) {
        super(category, price, description,)
    }
}